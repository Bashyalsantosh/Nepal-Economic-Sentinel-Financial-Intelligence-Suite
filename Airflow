from airflow import DAG
from airflow.operators.python import PythonOperator
from datetime import datetime, timedelta

default_args = {
    'owner': 'data_engineer',
    'retries': 2,
    'retry_delay': timedelta(minutes=5),
}

with DAG(
    dag_id='nepal_sentinel_etl_v1',
    default_args=default_args,
    start_date=datetime(2026, 1, 1),
    schedule_interval='0 18 * * 1-5', # Run daily after NEPSE close
    catchup=False
) as dag:
    # Define tasks for Extraction -> Spark Processing -> dbt Model Run
    pass
